export type formInfoTypes = {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  subject: string;
  message: string;
}
export type userTypes = {
  firstName: string;
  lastName: string;
  email: string;
  image?: string;
  _id: string;
}